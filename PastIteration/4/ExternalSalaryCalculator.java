public class ExternalSalaryCalculator
{
    public double calculateExternalSalary(double baseSalary, double bonus)
    {
        // Some third-party logic to calculate salary
        return baseSalary + bonus;
    }
}
